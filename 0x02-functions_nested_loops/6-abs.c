#include "main.h"

/**
 * _abs - main entry point
 * Description:a function that computes the absolute value of an integer\n
 * @b: the int to check
 * Return: int
 */
int _abs(int b)
{
	if (b >= 0)
	{
		return (b);
	}
	return (-b);
}
